// === Export IPC Handlers ===
// Handles all document export related IPC communication (placeholder for now)

function register(deps) {
  console.log('[ExportHandlers] Placeholder - handlers not yet migrated');
}

module.exports = {
  register
};