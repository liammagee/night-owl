// === AI Service IPC Handlers ===
// Handles all AI-related IPC communication (placeholder for now)

function register(deps) {
  console.log('[AIHandlers] Placeholder - handlers not yet migrated');
}

module.exports = {
  register
};