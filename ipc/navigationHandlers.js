// === Navigation IPC Handlers ===
// Handles all navigation and history related IPC communication (placeholder for now)

function register(deps) {
  console.log('[NavigationHandlers] Placeholder - handlers not yet migrated');
}

module.exports = {
  register
};