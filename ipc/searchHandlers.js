// === Search IPC Handlers ===
// Handles all search and replace related IPC communication (placeholder for now)

function register(deps) {
  console.log('[SearchHandlers] Placeholder - handlers not yet migrated');
}

module.exports = {
  register
};