// === File System IPC Handlers ===
// Handles all file system related IPC communication (placeholder for now)

function register(deps) {
  console.log('[FileHandlers] Placeholder - handlers not yet migrated');
}

module.exports = {
  register
};